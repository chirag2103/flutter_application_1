name: flutter_application_1
description: A multi-vendor construction application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  provider: ^6.0.5
  google_maps_flutter: ^2.3.0
  geolocator: ^9.0.2
  geocoding: ^2.1.1
  http: ^1.1.0
  shared_preferences: ^2.2.0
  image_picker: ^1.0.1
  cached_network_image: ^3.2.3
  flutter_stripe: ^9.2.2
  firebase_core: ^2.13.0
  firebase_auth: ^4.6.2
  cloud_firestore: ^4.8.0
  firebase_storage: ^11.2.2
  model_viewer_plus: ^1.5.0
  flutter_local_notifications: ^15.1.0
  socket_io_client: ^2.0.2
  google_fonts: ^6.1.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/
